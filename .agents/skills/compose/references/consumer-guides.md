# Targeted contracts

Read `docs/roblox.md` for the engine boundary, `docs/api.md` for public composition and diagnostics,
or `authoring/README.md` for semantic sets and project generation. The adopting application owns
its dependency pin and integration proof.
