# API

This note picks a Compose API for an application: read `docs/quickstart.md` before your first
component and `docs/api.md` before choosing a public signature. Pass a readable directly when no
transformation is needed. Use a reactive body only for a computation. For internal changes, stop
and follow the root `AGENTS.md`.
